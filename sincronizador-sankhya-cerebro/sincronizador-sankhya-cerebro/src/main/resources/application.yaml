spring:
  application:
    name: sincronizador-sankhya-cerebro

  datasource:
    url: jdbc:mariadb://localhost:3306/sankhya
    username: api_sankhya
    password: mariadb@Polpa#!2026
    driver-class-name: org.mariadb.jdbc.Driver
    hikari:
      maximum-pool-size: 5
      connection-timeout: 10000

  jackson:
    default-property-inclusion: non_null

server:
  port: 8080
  address: 0.0.0.0

# Chave que o botao de acao do Sankhya envia no header X-API-Key.
# Em producao, prefira ler de variavel de ambiente:
#   api-key: ${CEREBRO_API_KEY}
app:
  api-key: 95ptNRVaawehAbhMsWdEfuXXLfKDMVZ6Bx5GfPxCXSEBR7Fy

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    br.com.polpabrasil.sincronizador: INFO
  file:
    name: C:/DEV/logs/sincronizador.log
